# Scenario card assets

Exported from the user-supplied Passwork Figma file, node `572:8121`.

- `background.png`: isolated image node `572:8019`, 1352 × 743.
- `dashboard.png`: isolated image node `572:8119`, 1007 × 543.

The image nodes were exported directly because the design-context asset links
returned blank images. The website applies the reference background blur and
dark overlay in CSS and uses the shared site corner radius.
